import SectionTitle from "@/commons/section-title/SectionTitle";
import React from "react";

const Servicios = () => {
  return (
    <div style={{ justifyItems: "center", paddingTop: "30px" }}>
      <SectionTitle title={"Sección de servicios"} wordIndex={3} />
    </div>
  );
};

export default Servicios;
